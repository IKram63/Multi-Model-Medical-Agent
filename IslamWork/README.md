# Rib Fracture Classification Model

This project loads a pre-trained Keras model for rib fracture detection.

## Setup

1. Install dependencies:

pip install -r requirements.txt

2. Run the script:

python model.py

The trained model will be downloaded automatically from Google Drive if not found locally.